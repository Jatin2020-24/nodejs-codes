<!DOCTYPE html>
<html lang="en">
<head>
    <title>Profile Page</title>
</head>
<body>
    <h4>Welcome <%= user.name %></h4>
    <ul>
        <% user.skills.forEach((item)=>{ %>
            <li><%= item %></li>
        <% }) %>
    </ul>
</body>
</html>